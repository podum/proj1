import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.putadex',
  appName: 'putadex',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
